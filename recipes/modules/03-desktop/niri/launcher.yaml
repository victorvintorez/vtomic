# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json
modules:
  - type: dnf
    repos:
      cleanup: true
      copr:
        - scottames/vicinae
    install:
      packages:
        - vicinae

  - type: files
    files:
      - source: recipes/modules/03-desktop/niri/launcher
        destination: /
