# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json
# TODO: replace polkit agent
modules:
  - type: dnf
    repos:
      cleanup: true
      copr:
        - lbarrys/LBsWorld
    install:
      packages:
        - soteria

  - type: files
    files:
      - source: recipes/modules/03-desktop/niri/polkit
        destination: /
