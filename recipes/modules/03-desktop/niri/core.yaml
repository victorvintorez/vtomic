# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json
modules:
  - type: script
    snippets:
      - 'dnf install -y niri xwayland-satellite && find /usr/share/doc/niri -name "*[^[:ascii:]]*" -delete'

  - type: files
    files:
      - source: recipes/modules/03-desktop/niri/core
        destination: /
