# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json
modules:
  - type: dnf
    remove:
      packages:
        - grubby
        - grub2
        - bootupd

  - type: dnf
    install:
      packages:
        - systemd-boot-unsigned
        - sdubby
