# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json
# Add plymouth configuration
modules:
  - type: dnf
    install:
      packages:
        - xdg-user-dirs

  - type: files
    files:
      - source: recipes/modules/00-system/08-xdgdirs
        destination: /
