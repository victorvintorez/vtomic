# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json
modules:
  - type: dnf
    repos:
      cleanup: true
      copr:
        - atim/starship
    install:
      packages:
        - starship

  - type: files
    files:
      - source: recipes/modules/02-cli/starship
        destination: /
