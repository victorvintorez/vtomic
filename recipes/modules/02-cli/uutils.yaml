# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json
modules:
  - type: dnf
    install:
      packages:
        - uutils-coreutils

  - type: script
    scripts:
      - 02-cli/uutils/generate-nu-aliases.nu

  - type: files
    files:
      - source: recipes/modules/02-cli/uutils
        destination: /
