# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json
# TODO: write yubikey-touch-detector specfile
modules:
  - type: dnf
    install:
      packages:
        - libfido2
        - yubikey-manager

  - type: authselect
    source: local
    preset: local
    features:
      - with-pam-u2f-2fa
