# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json
modules:
  - type: dnf
    install:
      packages:
        - libfido2

  - type: authselect
    source: local
    preset: local
    features:
      - with-pam-u2f-2fa
