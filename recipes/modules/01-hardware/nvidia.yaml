# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json
modules:
  - type: akmods
    install:
      - v4l2loopback # Install is required so just define v4l2loopback again
    nvidia-driver: nvidia-open
